# Deprecation Candidates
- `apply_theme()` — intentionally deprecated (emits DeprecationWarning), retained as documented alias for `set_theme()`. Classification: **safe to keep** (deliberate back-compat alias, documented in CHANGELOG/API). Not for removal this release.
- `header()` method — deprecated alias for `set_header()` (emits warning). **Safe to keep** (back-compat).
(No unused/obsolete/dead code found in S1; revisit in S2/S5.)
